"use server";

export async function createImportJob(formData: FormData) {
  return { success: true };
}

export async function saveImportMapping(formData: FormData) {
  return { success: true };
}

export async function confirmImportRows(formData: FormData) {
  return { success: true };
}
