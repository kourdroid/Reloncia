# RLS Testing

This directory contains `pg_prove` or Supabase local SQL tests for Row Level Security (RLS) policies.

To run these tests, ensure you have a local Supabase instance running and the `pgtap` extension installed.

Run tests using:
```sh
supabase test db
```
